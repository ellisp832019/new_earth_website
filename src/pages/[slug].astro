---
import ContentPage from '../components/ContentPage.astro'; import { pages } from '../data/site';
export function getStaticPaths(){return Object.keys(pages).map(slug=>({params:{slug},props:{slug}}));} const {slug}=Astro.props;
---
<ContentPage slug={slug}/>
