# WordPress Material Status

This directory is **LEGACY / REFERENCE / ROLLBACK** material for the existing
WordPress publishing context.

It is not the current Website V2 implementation. Astro under `src/` is the
active Website V2 implementation and build system. Do not treat these files as
authority for current V2 routes, page design or deployment without an explicit
controlled instruction.
