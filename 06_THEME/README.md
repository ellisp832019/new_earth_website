# Theme Material Status

This directory is **LEGACY / REFERENCE / ROLLBACK** material for the former
WordPress theme implementation.

It is not the current Website V2 implementation. Astro under `src/` is the
active Website V2 implementation and build system. Preserve these files for
history and rollback reference; do not treat them as current V2 authority.
